class Solution:
    def strStr(self, haystack: str, needle: str) -> int:
        first_occurence = haystack.find(needle)
        
        return first_occurence
        

        # May have to be solved again